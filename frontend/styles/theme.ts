import { extendTheme } from '@chakra-ui/react'
export const theme = extendTheme({
  colors: { brand: { 50:'#e3f2ff',500:'#3182ce' } },
  fonts: { heading:'Inter, sans-serif', body:'Inter, sans-serif' }
})
